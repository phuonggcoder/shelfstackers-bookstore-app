// Vietmap integration was removed per project decision. This file intentionally
// contains a stub to avoid breaking imports elsewhere. Remove this file if
// you have cleaned up all references to Vietmap in the codebase.

export async function reverseGeocode(_lat: number, _lng: number) {
  return null;
}
